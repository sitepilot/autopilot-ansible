#!/bin/sh
# {{ autopilot_managed }}
set -e

# Reloads the web server when a .htaccess file has changed since last reload
if ! find /opt/sitepilot/users/ -maxdepth 6 -type f -newer /usr/local/lsws/cgid -name ".htaccess" -exec false {} +; then /usr/local/lsws/bin/lswsctrl restart; fi