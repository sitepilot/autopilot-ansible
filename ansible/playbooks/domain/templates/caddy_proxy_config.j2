# {{ sitepilot_managed }}
{{ domain }}, www.{{ domain }} {
    tls {{ email }} {
        on_demand
    }
    
    import config

    import webapps
  
    reverse_proxy {
        to {{ backends | join(' ') }}

        lb_policy first
        header_down -Server

        transport http {
            tls_insecure_skip_verify
        }
    }
}